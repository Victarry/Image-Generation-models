# The d_loss converges stablely, which can indicate the divergence between real image and generated images.
# @package _global_
defaults:
  - override /model: wgan
  - override /networks: conv_mnist
  - override /datamodule: mnist

trainer:
  max_epochs: 50
exp_name: wgan_mlp_mnist