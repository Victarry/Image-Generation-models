# @package _global_
defaults:
  - override /model: wgan_gp
  - override /networks: conv_64
  - override /datamodule: celeba

networks:
  encoder:
    batch_norm: False
  decoder:
    batch_norm: False

trainer:
  max_epochs: 50
exp_name: wgangp_conv_celeba