# @package _global_
defaults:
  - override /model: realnvp
  - override /datamodule: mnist

exp_name: realnvp/mnist

datamodule:
  transforms:
    grayscale: True
    normalize: True
trainer:
  max_epochs: 100
  check_val_every_n_epoch: 1