# @package _global_
defaults:
  - override /model: info_gan
  - override /networks: conv_mnist
  - override /datamodule: mnist

model:
  loss_mode: lsgan
exp_name: info_gan/baseline

trainer:
  max_epochs: 100