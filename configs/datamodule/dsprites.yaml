_target_: src.datamodules.dsprite.DataModule
data_dir: ${data_dir}
channels: 1
width: 64
height: 64
batch_size: 128
num_workers: 4
transforms:
  convert: True
  normalize: True
  resize:
    width: ${datamodule.width}
    height: ${datamodule.height}